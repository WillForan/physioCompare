# suma will display one niml.do and one 1d.do at a time
# - display nodes as niml and segements as 1d

### MAKE STUFF
# generate nodes
./createROINodes.bash
# generate segments
mkSegments.pl
# all of development
mkSegments.pl -p 1 -i ../txt/ageeffAgeXphys-invage-develROI.csv 

# all that are significant (threshold on t value of 2.59 (p~.01), sort on tval, color based on value
./mkSegments.pl -t 2.59 -i ../txt/ageeffAgeXphys-invage.csv -s ageXphysio.tval -v ageXphysio.val

### SEE STUFF
# launch afni, suma, load nodes and segments
./startSuma_chooseSegments.bash


## automaticly save stuff
./startSuma_shootSegments.bash
